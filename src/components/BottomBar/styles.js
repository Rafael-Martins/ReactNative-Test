import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  mainContainer: {
    backgroundColor: '#24242e',
    padding: 10,
  },
  iconContainer: {
    flexDirection: 'row',
  },
})
