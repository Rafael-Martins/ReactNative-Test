import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    background: {
        width: '100%',
        height: '100%',
        resizeMode: 'cover',
        alignItems: 'center',
    },
    backgroundContainer: {
        width: '100%',
        height: 200,
    },
    logo: {
        height: 26,
        width: 90,
        marginTop: 40,
        marginBottom: 30,
    },
})
